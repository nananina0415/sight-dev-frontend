# 빌드 결과물 (Docker 내부에서 새로 생성)
node_modules
dist
dist-ssr

# 환경변수 (민감 정보 제외)
.env.local
.env.*.local

# Yarn Berry 캐시 (레이어 낭비 방지)
.yarn/cache
.yarn/unplugged

# 버전 관리
.git
.gitignore

# 에디터
.vscode
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# 로그
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript 빌드 캐시
*.tsbuildinfo

# Docker 파일 자체 (컨텍스트 최소화)
Dockerfile
docker-compose*.yml
.dockerignore
